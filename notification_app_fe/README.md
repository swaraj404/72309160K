Stage 7 frontend (static)

Run the static server and visit http://localhost:3000

Commands:

Node (no deps):

```bash
node server.js
```

The page will call the backend at `http://localhost:5000/priority` to load the top-10 notifications.
